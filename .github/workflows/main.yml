name: Run simulation study for one datagen_scen
on: push
  paths:
    - ".github/workflow/input.txt"
#  schedule:
#    - cron:  '0 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: r-lib/actions/setup-r@v1
      #- name: Install Package Dependencies
        #run: |-
          #Rscript -e "install.packages('remotes')"
          #Rscript -e "remotes::install_deps(dependencies = TRUE)"
      - name: Run simulation study datagen_scen#1
        run: |-
          Rscript -e "source('rcode/testjob.R')" input.txt
      #- name: Create report file
       # run: date +%s > report.txt
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Add sim output simulation study datagen_scen#1
          committer: Linda Nab <LindaNab@users.noreply.github.com>
          branch: patch
      #- name: Commit results
        #run: |
          #git add "./data/helloworld.Rds"
          #git commit -m "Update data" -a || echo "No changes to commit"
          #git push #https://${{github.actor}}:${{secrets.GITHUB_TOKEN}}@github.com/${{github.repository}}.git HEAD:${{ github.ref }} || echo "No changes to commit"
